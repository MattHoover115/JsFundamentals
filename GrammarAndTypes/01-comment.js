// a single line comment

// using / / will comment out a single line, making this useful for notes

// We can use CTRL + / as a shortcut to make a comment


/* multi-line comments  */

/* 
using a / and a *
will create
a new multi-line
comment
*/

// In VSCode, our shortcut to make a comment will also work within HTML and CSS in the same way, though the syntax will be slightly different.

// comments are ignored when running code, anything inside of them will not be run, whether it is valid code or otherwise. This makes commenting code very useful when debugging

// lte myVan = 15;